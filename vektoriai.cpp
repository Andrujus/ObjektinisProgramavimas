#include "functions.h"
#include "student.h"

int main() {
    std::vector<Student> studentai;
    std::vector<int> skaicius = {1000, 10000, 100000, 1000000, 10000000};
    Duom(studentai);
    Rez(studentai);
    return 0;
}